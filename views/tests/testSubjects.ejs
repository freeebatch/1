<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Test Subjects</title>
    <style>
        /* General Page Styles */
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f9;
            color: #333;
        }

        header {
            text-align: center;
            padding: 20px;
            background: #007bff;
            color: white;
            font-size: 24px;
        }

        .container {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 20px;
            padding: 20px;
        }

        /* Card Styling */
        .card {
            background: white;
            border-radius: 8px;
            padding: 20px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            min-width: 320px;
            max-width: 400px;
            text-align: center;
            transition: transform 0.2s;
        }

        .card:hover {
            transform: translateY(-5px);
        }

        h2 {
            color: #007bff;
            font-size: 22px;
            margin-bottom: 10px;
        }

        aside {
            font-size: 16px;
            color: #555;
            margin-bottom: 10px;
        }

        /* Buttons */
        .card a {
            display: inline-block;
            margin: 10px 5px;
            padding: 10px 15px;
            background: #007bff;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            font-weight: bold;
            transition: background 0.3s ease;
        }

        .card a:hover {
            background: #0056b3;
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .container {
                flex-direction: column;
                align-items: center;
            }
        }
    </style>
</head>

<body>
    <header>
        Test Subjects
    </header>

    <div class="container">
        <% if (test && test.length>= 1) { %>
            <% test.forEach(subject=> { %>
                <div class="card">
                    <h2>
                        <%= subject.name %>
                    </h2>
                    <aside>
                        <%= subject.totalQuestions %> Questions |
                            <%= subject.totalMarks %> Marks |
                                <%= subject.maxDuration %> Mins
                    </aside>
                    <aside>
                        <strong>Held on:</strong>
                        <%= subject.startTime %>
                    </aside>
                    <% if(!saved){ %>
                        <a href="/test/test-service/<%= subject.testId %>/instructions">📖 Instructions</a>
                        <a
                            href="/test/test-service/<%= subject.testId %>/categoryModeId/<%= testModeId %>/testStudentMappingId/<%= subject.testStudentMappingId %>/questions">
                            🚀 Take Test
                        </a>
                        <% } else { %>
                            <a
                                href="/test/saved/test-service/<%= subject.testId %>/categoryId/<%= categoryId %>/testModeId/<%= testModeId %>/instructions">📖
                                Instructions</a>
                            <a
                                href="/test/saved/test-service/<%= subject.testId %>/categoryId/<%= categoryId %>/testModeId/<%= testModeId %>/questions">🚀
                                Take Test</a>
                            <% } %>
                </div>
                <% }); %>
                    <% } %>
    </div>

</body>

</html>